# Frontend Notes

This folder contains the React + Vite client for SmartSeason.

For full project documentation (setup, architecture, assumptions, demo credentials, and API notes), see the root README:

- `../README.md`

Quick commands from this folder:

```bash
npm install
npm run dev
```
